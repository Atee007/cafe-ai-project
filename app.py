import streamlit as st
import pandas as pd
import plotly.express as px

# 1. โค้ดส่วนการตั้งค่าหน้ากระดาษและธีมสี (CSS Customization)
st.set_page_config(layout="wide")

st.markdown("""
    <style>
    /* เปลี่ยนสีพื้นหลังทั้งเว็บเป็นสีครีม */
    .stApp {
        background-color: #FDF8F1;
    }
    /* ปรับแต่งแถบเมนูข้างซ้าย (Sidebar) */
    [data-testid="stSidebar"] {
        background-color: #5D4037; /* สีน้ำตาลเข้ม */
    }
    [data-testid="stSidebar"] .stText, [data-testid="stSidebar"] label {
        color: white;
    }
    /* ปรับแต่งกล่อง Card (KPI) ให้มีพื้นหลังขาวและขอบมน */
    .stMetric {
        background-color: white;
        padding: 20px;
        border-radius: 15px;
        box-shadow: 2px 2px 10px rgba(0,0,0,0.05);
    }
    </style>
    """, unsafe_allow_html=True)

# 2. โค้ดส่วนเมนูนำทางด้านซ้าย (Sidebar Navigation)
with st.sidebar:
    st.title("☕ Cafe Sales")
    st.write("ระบบติดตามยอดขาย")
    menu = st.radio(
        "เมนูหลัก",
        ["แดชบอร์ด", "บันทึกยอดขาย", "จัดการสินค้า", "คาดการณ์ยอดขาย", "ตั้งค่า"]
    )
    st.sidebar.markdown("---")
    st.sidebar.write("เวอร์ชัน 1.0 • Student Project")

# 3. โค้ดส่วนการแสดงผลตามเมนู (Logic)
if menu == "แดชบอร์ด":
    st.title("ภาพรวมยอดขาย")
    
    # แบ่งเป็น 5 คอลัมน์สำหรับวาง KPI เหมือนในรูป
    col1, col2, col3, col4, col5 = st.columns(5)
    with col1:
        st.metric("ยอดขายวันนี้", "฿2,720", "+5%")
    with col2:
        st.metric("จำนวนบิล", "835", "+2%")
    with col3:
        st.metric("เฉลี่ยต่อบิล", "฿670", "-1%")
    with col4:
        st.metric("ยอดขายเดือนนี้", "฿2,909.24", "+12%")
    with col5:
        st.metric("แนวโน้ม", "+6.0%", "Good")

    # ส่วนพื้นที่วางกราฟ (ตัวอย่าง)
    st.write("### กราฟแสดงแนวโน้ม")
    # อาจารย์เอาโค้ดกราฟเดิมของอาจารย์มาวางตรงนี้ได้เลยครับ

elif menu == "คาดการณ์ยอดขาย":
    st.title("🤖 ระบบคาดการณ์อัจฉริยะ (AI Prediction)")
    # อาจารย์เอาโค้ดส่วนที่ใช้ XGBoost หรือ Model AI มาวางตรงนี้ครับ

# ส่วนเมนูอื่นๆ อาจารย์สามารถเขียนเงื่อนไขเพิ่มได้ตามต้องการ
